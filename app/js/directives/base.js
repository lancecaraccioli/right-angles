'use strict';

/*
angular.module('AngularChromeAppShowcase').
    directive('directiveName', ['injectedDependency', function (injectedDependency) {
        return function (scope, element) {
            //element.text(injectedDependency.doSomething());
        };
    }]);
*/

